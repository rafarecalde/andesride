---
import { u } from '../lib/url';
import { heroPhoto } from '../lib/hero';
import Booker from './Booker.astro';
---
<section class:list={['hero', heroPhoto && 'has-photo']} id="top">
  <div class="hero-bg" aria-hidden="true">
    {heroPhoto
      ? <img src={heroPhoto.src} alt="" width={heroPhoto.width} height={heroPhoto.height} decoding="async" fetchpriority="high" />
      : <img src={u('/hero-quito-placeholder.svg')} alt="" />}
  </div>
  <div class="hero-overlay" aria-hidden="true"></div>
  <div class="wrap">
    <div class="hero-grid">
      <div class="hero-copy">
        <span class="eyebrow">Quito Airport Transfer · UIO</span>
        <h1>Prepaid luxury SUV from UIO</h1>
        <p class="hero-sub">Licensed driver. $50 to Wyndham Quito Airport, $100 anywhere in Quito — Hyatt, Oro Verde, Swissôtel, Casa Gangotena.</p>
        <div class="hero-prices" aria-label="Prepaid flat rates">
          <div class="hp"><b>$50</b><span>Wyndham Quito Airport</span></div>
          <div class="hp"><b>$100</b><span>Anywhere in Quito</span></div>
        </div>
        <ul class="hero-trust">
          <li>Licensed driver</li>
          <li>Prepaid in USD</li>
          <li>Flight tracking</li>
        </ul>
      </div>

      <Booker />
    </div>
  </div>
</section>
